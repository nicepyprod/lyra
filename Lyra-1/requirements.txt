# Core dependencies for Lyra-1 training and inference

# Deep learning framework
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Hugging Face ecosystem
transformers>=4.37.0
diffusers>=0.25.0
accelerate>=0.26.0
peft>=0.7.0
tokenizers>=0.15.0
datasets>=2.16.0
huggingface-hub>=0.20.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0
albumentations>=1.3.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Scientific computing
numpy>=1.24.0
scipy>=1.11.0
einops>=0.7.0

# Training utilities
tqdm>=4.65.0
tensorboard>=2.14.0
wandb>=0.16.0

# Configuration management
omegaconf>=2.3.0
hydra-core>=1.3.0

# Evaluation metrics
torchmetrics>=1.2.0
lpips>=0.1.4

# Data handling
pandas>=2.0.0
pyarrow>=13.0.0

# Utilities
requests>=2.31.0
click>=8.1.0
rich>=13.0.0
python-dotenv>=1.0.0

# Jupyter / notebook support (useful for local experimentation)
jupyterlab>=4.0.0
ipywidgets>=8.0.0

# Optional: Flash Attention for faster training
# flash-attn>=2.3.0  # Requires CUDA, install separately if needed

# Optional: xFormers for memory-efficient attention
# xformers>=0.0.23  # Install separately based on CUDA version

# NOTE: On my local setup (RTX 3090, CUDA 12.1) I install flash-attn manually:
# pip install flash-attn==2.5.6 --no-build-isolation
# This gave a noticeable speedup during fine-tuning runs.
